apiVersion: v1
kind: Service
metadata:
  name: {{ include "lemp_k8s_chart.fullname" . }}
  labels:
    {{- include "lemp_k8s_chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lemp_k8s_chart.selectorLabels" . | nindent 4 }}
