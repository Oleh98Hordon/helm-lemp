apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helmlemp.fullname" . }}-test-connection"
  labels:
    {{- include "helmlemp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helmlemp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
